# 个人简历定向真实面经题单

> 筛选依据：`个人简历.txt`、`真实面经.txt`、`云存储真实面经.txt`，以及 `D:\Desktop\AI_YunCunChu` 当前代码。
>
> 适用方向：C++ 开发、Linux/C++、服务器后端、嵌入式 Linux 应用层。AI 功能属于后续项目迭代，本轮不进入项目介绍、项目亮点和项目问答口径。
>
> 使用原则：先回答当前实际实现，再说明缺口，最后才谈改进方案；不能把框架能力、论文方案或计划中的功能说成自己的已实现成果。

## 一、筛选结论

### 1. 原始材料规模

- `真实面经.txt`：4733 行，其中 4538 行非空。
- `云存储真实面经.txt`：126 行，包含 109 个编号项以及前置未编号问题，存在重复题和 AI 迭代题。
- 两份材料按关键词匹配得到下表。各类别会重叠，因此这里只反映题目密度，不能相加当作题目总数。

| 主题            | 匹配行数 | 对本简历的意义                    |
| ------------- | ---: | -------------------------- |
| C++ 语言、STL、内存 |  679 | 技能栏直接触发，必须系统准备             |
| 项目与答辩         |  473 | 没有实习经历时，项目会被重点验真           |
| Linux、操作系统、并发 |  435 | 技能栏覆盖很广，容易连续深挖             |
| 网络协议          |  233 | 课程、Socket、TCP/IP 与项目链路共同触发 |
| 综合与 HR        |  224 | 本科、独立开发、项目来源和职业规划是重点       |
| 算法与手撕         |  182 | C++ 岗基本无法跳过                |
| MySQL、Redis   |  161 | 项目真实使用，不能只背定义              |
| 云存储专项         |  152 | 唯一简历项目，优先级最高               |
| 部署与网关         |  114 | Docker、Nginx、FastCGI 都会被追问 |
| AI 相关         |  342 | 本轮按要求排除，不并入项目回答            |

### 2. 最终优先级

| 优先级 | 范围                        | 目标                            |
| --- | ------------------------- | ----------------------------- |
| S0  | 简历真实性和项目边界                | 任何一句都不能被代码反证                  |
| S1  | 云存储完整链路与失败场景              | 能从客户端一直讲到 FastDFS、MySQL、Redis |
| S2  | C++、Linux、网络高频基础          | 能承受连续三层追问，而不是只答名词             |
| A   | MySQL、Redis、Docker、Nginx  | 能结合项目数据和配置回答                  |
| A   | 高频手撕                      | 20～30 分钟内写对并讲测试用例             |
| B   | HR、学习过程、职业规划              | 用真实经历组织 STAR 回答               |
| 暂缓  | AI 项目、Qt/ROS/音视频/驱动/K8s 等 | 除非目标 JD 明确要求，否则不占主复习时间        |

## 二、投递前必须处理的简历风险

这是整份题单中优先级最高的部分。真实面经里已经出现“是不是你做的”“修改了哪些源码”“指标怎么采集”“提升比例如何测出”“有没有提交社区”等连环追问。

| 当前简历表述 | 当前代码事实 | 面试风险 | 建议口径 |
| --- | --- | --- | --- |
| 改进了 FastDFS 原负载均衡算法 | 当前仓库没有 Tracker 调度修改、指标采集、构建接入或压测代码 | 极高，一旦要求画决策逻辑或展示改动就会失真 | 改成“阅读并调研 FastDFS 调度策略与动态反馈方案，形成改进设计，尚未合入当前工程” |
| 对负载均衡和缓存管理设计并实现优化并进行了性能测试 | 当前仓库没有 LRU-K、Storage 缓存模块和可信基线/对照实验 | 极高，会被追问数据集、硬件、P95、错误率和原始结果 | 删除“实现、性能测试”或补齐实现与可复现实验后再写 |
| Redis 存储热点数据 | 当前实际主要保存 Token、分片会话、公共分享排行和文件名映射 | 中高，“热点数据是什么”会暴露口径不清 | 明确写为“保存登录 Token、分片上传状态和分享排行缓存” |
| 分片全部成功后合成完整文件再存储 | 当前首片创建 FastDFS appender 文件，后续分片按序追加；不是在本地形成完整文件后一次上传 | 高，会被追问内存和临时目录 | 改为“临时保存分片，按编号使用 FastDFS appender API 顺序追加” |
| Nginx 作为反向代理 Web 服务器 | 当前主要是 HTTPS 入口、静态资源、13 条 FastCGI 路由和 FastDFS 文件访问；无 `upstream` | 中，容易被误解为已做应用层负载均衡 | 回答时明确“使用 `fastcgi_pass` 分发业务路由，当前未配置 upstream 集群” |
| 支持一定数量客户端连接和及时响应 | 当前没有可信并发压测结果 | 中，会被追问具体并发数和 P95 | 不给数字；说明 Nginx/FastCGI 架构能力与当前实测结果是两回事 |
| 熟悉线程池、连接池、内存池等组件 | 云存储业务源码没有这些池化组件 | 中高，会被问“项目哪一处用了” | 明确是独立练习或原理学习，不绑定云存储项目 |
| 熟练使用 AI 工具 | 会触发“项目到底哪些代码是你写的” | 中高 | AI 项目功能本轮不讲；若保留此技能，必须能说明需求拆分、审查、测试和最终责任边界 |

### 必须能直接回答的四个验真问题

1. 这个项目是学习项目、课程项目还是实际业务？有没有真实用户和上线数据？
2. FastDFS 已经实现存储，你自己真正实现了哪些业务链路？
3. 项目后端主要是 C 还是 C++？为什么简历又以 C++ 为主要技术方向？
4. 哪些是当前代码已经完成的，哪些只是读过论文、做过设计或计划迭代？

推荐统一回答：

> 这是我独立完成的学习型云存储项目，当前以单机 Docker Compose 方式部署，没有生产用户数据。FastDFS 负责文件正文存储，我主要完成了注册登录、秒传判断、普通上传、分片上传与 appender 追加、文件列表、分享转存和引用计数删除等业务链路，并接入 MySQL、Redis、Nginx 与 FastCGI。传统业务模块主要是 C，不会说成全部由 C++ 编写。负载均衡和 LRU-K 是我阅读资料后形成的改进方向，当前复核仓库没有正式接入，所以不会当作已实现成果。

## 三、S0：面试开场必备 12 题

这些题决定面试官后续往哪个方向深挖，应先准备成口语答案。

1. 请做一段 60～90 秒的自我介绍。
2. 简历中哪个项目最值得讲？为什么？
3. 为什么选择做云存储项目？项目来源和应用场景是什么？
4. 用两分钟介绍项目架构、核心功能和个人贡献。
5. FastDFS 已经提供文件存储，你的工作量和技术价值在哪里？
6. 这个项目是独立开发还是团队开发？如何证明自己理解全部链路？
7. 项目实际部署在哪里？用了几台机器、几个容器、几个 Tracker 和 Storage？
8. 项目是否上线、压测过？并发量、吞吐量和延迟是多少？
9. 项目中最难的问题是什么？发现、定位和解决过程是什么？
10. 项目中印象最深的 Bug 是什么？如何复现和定位？
11. 如果重新设计项目，最先改哪三处？为什么？
12. 为什么投 C++ 岗，而当前云存储主业务模块主要是 C？

回答边界：

- 第 7 题：当前是单机三容器，只有一个 Tracker、一个 Storage，不是高可用集群。
- 第 8 题：没有可信压测就直接说没有，不能编并发量或性能提升比例。
- 第 9 题：适合讲分片状态、临时文件、FastDFS appender 与数据库发布之间的失败恢复问题。
- 第 10 题：必须选择自己能用代码位置、日志或复现步骤说清的真实问题。
- 第 12 题：C++ 是主学习方向；项目让自己补足了 C、Linux 进程、FastCGI 和系统集成能力。不要把 C 项目硬说成 C++ 项目。

## 四、S1：云存储项目定向题单

> 更贴近真实面试的精简版见 `云存储项目真实面试题单.md`。本节保留原有完整问题，便于复盘。
>
> 标记说明：`【源码复盘】` 表示问题依赖简历没有公开的接口、字段、端口、函数或当前代码缺陷。面试官只有提前看过源码，或在你主动透露该细节后，才会自然追问；不应作为第一轮背诵重点。

### 1. 架构、部署和模块

1. 从客户端、Nginx、FastCGI、FastDFS、MySQL、Redis 六个角色介绍系统。
2. Docker Compose 中有几个服务？为什么“三个容器”不等于“三台服务器”？
3. 【源码复盘】Nginx 的 13 条 API 路由如何映射到 13 个 FastCGI 程序？
4. 普通 API、文件上传、文件下载分别走什么链路？
5. Tracker 和 Storage 各做什么？文件正文为什么不经过 Tracker？
6. Group 之间是分片关系还是副本关系？同组 Storage 之间是什么关系？
7. 当前部署有几个副本？一台 Storage 下线后实际会怎样？
8. 所有 Tracker 都坏了会怎样？已有文件是否立刻丢失？
9. 为什么选择 FastDFS，而不是直接保存在 Nginx 所在机器的本地目录？
10. 是否调研过对象存储、MinIO 或其他分布式文件系统？选择标准是什么？
11. Nginx 和 FastDFS 如何对接？`ngx_fastdfs_module` 起什么作用？
12. Nginx 当前有没有负载均衡？`fastcgi_pass` 和 `upstream` 有什么区别？

### 2. 一次完整上传

13. 上传一个小文件时，客户端、Nginx、FastCGI、FastDFS、MySQL 和 Redis 如何串起来？
14. 【源码复盘】`/api/md5` 有哪几个分支？用户已有、全局存在和完全不存在分别怎样处理？
15. 普通上传和秒传的本质区别是什么？秒传为什么仍然需要写数据库？
16. 前端 MD5 是针对完整文件还是每个分片？大文件计算 MD5 会不会占用大量内存？
17. 当前服务端是否重新计算内容 MD5？只相信客户端 MD5 有什么风险？
18. 两个不同文件发生 MD5 碰撞怎么办？换 SHA-256 后还需要校验大小吗？
19. 两个用户同时上传相同文件，当前代码是否可能重复上传？竞态发生在哪里？
20. 【源码复盘】普通上传为什么先写本地临时文件？之后如何调用 `fdfs_upload_file`？
21. 【源码复盘】`fork`、`exec` 和管道在上传模块中各自做什么？子进程失败怎样传递？
22. FastDFS 上传成功而 MySQL 写入失败会怎样？如何清理孤儿文件？
23. MySQL 成功而 Redis 更新失败会怎样？哪些 Redis 数据能重建？
24. 怎样用唯一键、内容占位状态、事务和补偿任务改造上传链路？

### 3. 大文件分片和断点续传

25. 为什么大文件要分片？当前阈值和分片大小分别是多少？
26. 1000 个分片会一次性全部提交给 Nginx 吗？当前前端是串行还是并发？
27. 【源码复盘】分片上传前为什么需要初始化会话？Redis Hash 中保存哪些字段？
28. 分片在合并前保存在哪里？容器重启后还能恢复吗？
29. 【源码复盘】不同文件的分片编号都从 0 开始，如何避免相互覆盖？只用 MD5 作目录名有什么风险？
30. 【源码复盘】当前单片上传接口是否验证 Token、会话所有者、分片范围、分片大小和分片摘要？
31. 【源码复盘】当前怎样记录已上传分片？逗号字符串在并发上传时有什么问题？
32. 分片乱序到达会影响最终内容吗？“TCP 保序”和“业务分片有序”有什么区别？
33. 【源码复盘】合并前如何判断分片完整？当前代码只检查存在性还缺少哪些验证？
34. 【源码复盘】首片和后续分片分别调用什么 FastDFS API？为什么使用 appender 文件？
35. 合并时会不会把整个文件读入内存？服务器内存不足应怎样流式处理？
36. 【源码复盘】为什么边追加边删除本地分片会使失败重试困难？
37. 【源码复盘】网络中断后当前能恢复到什么程度？为什么不能说是跨容器可靠断点续传？
38. 多个合并请求同时到达会怎样？如何设计合并锁和状态机？
39. 为什么记录分片集合通常比只记录已上传字节数更合适？
40. 多个用户同时上传大文件时，怎样做并发限制、背压、磁盘配额和过期清理？

### 4. MySQL、Redis、分享和删除

41. 【源码复盘】六张传统业务表分别保存什么？`file_info` 和 `user_file_list` 为什么要拆开？
42. 一个用户文件与一份物理正文是什么关系？引用计数维护什么不变量？
43. Redis 在项目中实际保存哪三类数据？为什么不能笼统说成“缓存热点文件”？
44. Token 的 Key、Value 和 TTL 是什么？为什么不是 JWT 和滑动过期？
45. MySQL 和 Redis 谁是事实源？Token、分片会话、分享排行的答案是否相同？
46. 公共分享排行为什么使用 ZSet？member 和 score 各是什么？
47. 分享、取消分享、转存分别修改哪些表和缓存？
48. 多用户秒传后共享同一正文，其中一个用户删除文件时怎样处理？
49. 引用计数降为零后，数据库记录与 FastDFS 文件按什么顺序删除？失败如何补偿？
50. 【源码复盘】当前哪些分享或写接口缺少 Token 校验？可能造成什么越权？
51. 文件 URL 暴露真实路径怎么办？如何设计短链接、签名 URL 和过期时间？
52. 如果要限制分享链接的访问次数，怎样保证高并发下不超发？当前项目是否实现？
53. 【源码复盘】图片分享的提取码当前是否真正参与访问校验？

### 5. 性能、高可用和工程边界

54. 当前单 Tracker、单 Storage、单应用实例有哪些单点？
55. 如果扩成高可用，Nginx、FastCGI、MySQL、Redis 和 FastDFS 各层怎样改？
56. FastDFS 默认节点选择策略有哪些？不能只看剩余空间的原因是什么？
57. 如果设计动态反馈调度，应采集哪些指标、多久更新一次、怎样避免指标抖动？
58. 磁盘利用率、磁盘服务时间、CPU、活动连接和任务队列如何量化？
59. 怎样隔离网络带宽对存储调度对照实验的影响？
60. LRU 和 LRU-K 有什么区别？LRU-K 为什么能减轻一次性扫描污染？
61. 当前仓库是否实现 LRU-K 和动态负载均衡？有什么代码或实验依据？
62. FastDFS 属于开源框架，你是否修改过官方源码、提交过 PR 或复现过论文？
63. 如果没有真实集群和原始压测数据，面试中怎样诚实讨论优化方案？

项目题参考答案集中在 `八股面试题答案整理.md` 的第 249～308 题；更详细的真实代码链路见 `网盘项目架构与HTTP接口学习文档.md`。二者冲突时，以当前代码事实为准。

## 五、S2：C++ 定向题单

简历用了“熟悉 C++11/14/17”“熟悉封装、继承、多态和 STL”，真实面经会默认继续追问底层、边界和使用场景。

### 1. 对象模型和多态

1. 封装、继承、多态分别解决什么问题？静态多态和动态多态怎样实现？
2. 虚函数为什么能实现动态绑定？对象、虚表和虚表指针是什么关系？
3. 虚表是每个类一份还是每个对象一份？对象大小如何受虚函数影响？
4. 构造函数为什么不能是虚函数？析构函数为什么经常需要是虚函数？
5. 虚函数、纯虚函数、重载、重写和隐藏分别是什么？
6. 多继承下同名虚函数会有什么歧义？菱形继承怎样处理？
7. 为什么工程实践中常说“组合优于继承”？
8. C++ 四种显式类型转换分别适用于什么场景？

### 2. RAII、智能指针和值类别

9. `unique_ptr`、`shared_ptr`、`weak_ptr` 的所有权模型是什么？
10. `shared_ptr` 控制块保存什么？引用计数什么时候增减？
11. `shared_ptr` 的引用计数线程安全是否等于所指对象线程安全？
12. 循环引用如何产生？为什么 `weak_ptr` 可以解决？
13. 智能指针能否和裸指针混用？重复构造控制块会发生什么？
14. 左值、右值、左值引用和右值引用怎样区分？
15. `std::move` 做了什么？移动后对象处于什么状态？
16. 移动构造、移动赋值、引用折叠和完美转发分别解决什么问题？
17. Lambda 的值捕获、引用捕获和 `mutable` 是什么？异步线程引用捕获局部变量有什么风险？

### 3. STL、内存和编译

18. `vector` 的底层结构、扩容过程和迭代器失效规则是什么？
19. `resize` 与 `reserve` 的区别是什么？为什么不能笼统说 vector 每次两倍扩容？
20. `vector` 与 `list` 如何选择？连续内存为什么通常缓存友好？
21. `map` 与 `unordered_map` 的底层结构、复杂度、顺序性和失效规则是什么？
22. 自定义类型作为 `map` 的 Key 或 `unordered_map` 的 Key 需要提供什么？
23. `push_back` 与 `emplace_back` 有什么区别？是否一定后者更快？
24. `std::sort` 通常怎样实现？快速排序为什么会退化？
25. `new/delete` 与 `malloc/free` 有什么区别？失败行为分别是什么？
26. 深拷贝和浅拷贝是什么？Rule of Three/Five/Zero 怎样理解？
27. C++ 进程的代码段、数据段、BSS、堆、栈和映射区分别放什么？
28. 内存对齐为什么存在？结构体怎样计算大小？
29. 常见内存泄漏、越界、野指针和重复释放怎样发现与避免？
30. 源代码到可执行文件经历哪些阶段？静态库与动态库有什么区别？
31. `static`、`const`、`volatile`、`inline`、`explicit` 分别有什么作用？
32. `auto` 推导规则是什么？数组、引用、顶层 const 会怎样变化？
33. C++11/14/17 各选三项你真正使用过的特性说明场景。

回答边界：如果云存储业务代码没有使用某个 C++ 特性，要回答“这是语言基础或独立练习”，不能编造项目使用点。

## 六、S2：Linux、操作系统与并发题单

1. 进程和线程的区别是什么？创建、地址空间、资源和切换成本怎样比较？
2. 进程、线程和协程有什么区别？协程为什么不是操作系统调度单位？
3. Linux 有哪些进程间通信方式？各自适合什么场景？
4. 匿名管道怎样工作？云存储上传模块为什么可用它接收子进程输出？
5. `fork` 后父子进程拥有哪些相同和不同的资源？什么是写时复制？
6. `exec` 成功后原进程的哪些内容被替换？失败怎样处理？
7. 线程同步有哪些方式？互斥锁、自旋锁、读写锁、条件变量怎样选择？
8. 条件变量为什么必须与互斥锁和条件谓词配合？怎样处理虚假唤醒？
9. 死锁四个必要条件是什么？怎样预防、避免、检测和恢复？
10. 同一线程连续锁两次普通互斥量会怎样？递归锁的代价是什么？
11. 线程池由哪些组件组成？任务队列满了应采用什么拒绝或背压策略？
12. 如何实现有界、线程安全的生产者消费者队列？
13. `select`、`poll`、`epoll` 的数据结构和复杂度有什么区别？
14. epoll 的 LT 和 ET 有什么区别？ET 下怎样保证不漏读？
15. Reactor 和 Proactor 的职责分工有什么区别？Nginx 为什么性能高？
16. 什么是零拷贝？传统 `read+write` 与 `sendfile` 的数据路径有什么区别？
17. Linux 进程 CPU 或内存异常时怎样用 `top`、`ps`、`pmap` 和 `/proc` 排查？
18. 程序段错误后怎样生成并用 GDB 加载 core dump？
19. 如何用 GDB 查看线程、调用栈、变量、内存和死锁位置？
20. 如何用 `ss`/`netstat`、`lsof`、`tcpdump`、`ping`、`traceroute` 分层排查网络问题？

项目关联边界：当前云存储使用了多进程式的常驻 FastCGI 程序和上传链路中的 `fork/exec/pipe`；没有业务线程池、连接池和内存池实现。

## 七、S2：计算机网络题单

1. TCP 与 UDP 的区别是什么？云存储的 HTTP 上传底层为什么使用 TCP？
2. TCP 三次握手的过程是什么？为什么不是两次？`connect`、`listen`、`accept` 与握手是什么关系？
3. TCP 四次挥手的状态变化是什么？为什么常见四次而不是三次？
4. `TIME_WAIT` 出现在哪一端？为什么等待 2MSL？过多时怎样分析？
5. TCP 如何保证可靠性？至少说出序号、确认、重传、校验、流量控制和拥塞控制。
6. 滑动窗口和拥塞窗口分别解决什么问题？发送窗口受谁限制？
7. 慢启动、拥塞避免、快速重传和快速恢复怎样工作？
8. TCP 为什么会有粘包/拆包问题？HTTP 或自定义协议怎样确定消息边界？
9. TCP 能保证字节流有序，为什么业务上的文件分片仍需分片编号？
10. Socket 服务端与客户端的完整 API 调用流程是什么？
11. 浏览器输入 HTTPS URL 后，从 DNS 到页面响应经历哪些步骤？
12. HTTP GET 与 POST 的语义和常见使用区别是什么？不能用“GET 一定没有 Body”作绝对回答。
13. HTTP 常见状态码、长连接和 Range 断点下载怎样工作？
14. HTTPS/TLS 握手怎样结合非对称加密、对称加密、证书和摘要？
15. DNS 解析过程是什么？为什么 ping 不通不等于 HTTP 服务不可用？
16. 服务端启动但请求没有响应，怎样从 DNS、路由、端口、TLS、Nginx、FastCGI 到存储逐层排查？

回答边界：简历写了“熟悉 Socket 编程”，但当前网盘请求主要由 Nginx/FastCGI 接入。可以讲独立 Socket 练习，不能说网盘业务自己手写了 TCP 服务器。

## 八、A：MySQL、Redis、Nginx 与 Docker 题单

### 1. MySQL

1. 项目六张主链路表之间怎样关联？为什么没有把所有信息放在一张表？
2. InnoDB 为什么使用 B+ 树？与 B 树和红黑树相比有什么优势？
3. 聚簇索引、二级索引和回表是什么？覆盖索引如何减少回表？
4. 联合索引的最左前缀原则是什么？范围查询后的列还能怎样使用索引？
5. 常见索引失效场景有哪些？如何用 `EXPLAIN` 验证而不是凭感觉？
6. 事务的 ACID 是什么？四种隔离级别分别解决哪些并发现象？
7. MVCC、Read View、undo log 怎样支持一致性读？
8. redo log、undo log 和 binlog 的作用及写入时机有什么区别？
9. 间隙锁与 next-key lock 解决什么问题？什么时候会扩大锁范围？
10. 项目多表更新为什么需要事务？事务能否直接覆盖 FastDFS 和 Redis？

### 2. Redis

11. Redis 为什么快？“单线程”准确指什么？
12. String、Hash、List、Set、ZSet 分别适合什么场景？项目为什么使用 Hash 和 ZSet？
13. ZSet 的底层结构是什么？分享排行榜怎样分页和更新分数？
14. RDB 和 AOF 有什么区别？当前 Token 或分片会话丢失后分别有什么影响？
15. 缓存穿透、击穿和雪崩是什么？各自怎样治理？
16. Cache Aside 中先更新数据库还是先删缓存？失败时怎样补偿？
17. Redis 分布式锁为什么需要原子加锁、唯一值、过期和 Lua 解锁？
18. 项目是否真正需要“Redis 与 MySQL 强一致”？应先区分事实源和可重建缓存。

### 3. Nginx 与 Docker

19. Nginx master/worker 模型是什么？事件驱动和 epoll 怎样支撑并发？
20. 反向代理、动静分离、FastCGI 转发和负载均衡分别是什么？
21. 当前配置的 `client_max_body_size 12m` 与 10 MiB 分片有什么关系？
22. Docker 与虚拟机的隔离边界有什么区别？Namespace、cgroup 和分层文件系统各做什么？
23. Dockerfile 怎样做多阶段构建、减少层数和缩小镜像？
24. Compose 怎样编排三类服务？数据卷、健康检查和启动依赖分别解决什么？

## 九、A：手撕题最小集合

原始面经中“手撕”出现频繁，但无需无差别刷题。优先保证下面 15 类可以独立写出、解释复杂度并设计边界测试。

1. 反转链表；区间反转；K 个一组反转。
2. 判断链表是否有环并找到环入口。
3. 合并两个或 K 个有序链表。
4. 两个栈实现队列，或两个队列实现栈。
5. 快速排序、归并排序和堆排序。
6. 二分查找及旋转数组最小值。
7. Top K 高频元素或第 K 大元素。
8. 二叉树层序遍历、最大深度、左右视图和最近公共祖先。
9. 最长无重复子串与最小覆盖子串。
10. 三数之和、合并区间、岛屿数量。
11. LRU Cache；进阶为带 TTL 的 LRU。
12. 线程安全生产者消费者队列。
13. 线程安全单例。
14. 简单哈希表并处理冲突。
15. 字符串反转、单词反转、括号匹配和版本号比较。

每道题完成后强制回答：

- 时间复杂度和空间复杂度是什么？
- 空输入、单元素、重复值、极值和非法输入怎样处理？
- 哪些迭代器、指针或引用可能失效？
- 若并发调用，哪里会产生数据竞争？
- 如何用 3～5 个测试用例快速自证？

## 十、B：个人经历与 HR 定向题

1. 为什么没有实习经历？你如何证明工程能力？
2. 独立开发项目时怎样拆需求、排优先级和验收？
3. 项目开发了多久？每个阶段具体完成了什么？
4. 没有导师或团队时，遇到问题怎样求证，而不是只复制网络答案？
5. 举一个主动发现问题并推进解决的例子。
6. 举一个长期调试仍失败、最后改变思路解决的例子。
7. 如何看待自己项目当前存在的安全和一致性缺口？
8. 为什么选择 C++/Linux 方向？未来三年的技术路线是什么？
9. 目标城市、到岗时间、薪资预期和是否接受加班怎样回答？
10. 你最大的优点和缺点是什么？各给一个可验证事例。
11. 课程中哪一门掌握最好？怎样应用到项目？
12. 平时如何学习开源项目？是否做过 Issue、PR 或最小复现？
13. 如何与同学或测试人员沟通一个难以复现的问题？
14. 如果面试官指出项目设计错误，你怎样验证和回应？
15. 反问面试官什么问题能判断岗位是否适合自己？

建议准备三个真实 STAR 事件：一次复杂 Bug、一次主动学习解决问题、一次计划失误后的修正。没有团队经历时不要虚构协作案例，可使用课程设计、竞赛或同学代码评审中的真实事件。

## 十一、本轮明确降级或排除的题目

### 1. AI 项目题全部排除

包括 AI 搜索助手、RAG、向量数据库、模型选择、Agent、MCP、提示词、幻觉治理和文件向量化等。原因是 AI 属于项目后续迭代，用户已明确暂不进入回答口径。

需要注意：简历技能栏仍写有 Codex、Claude Code、Trae。即使不讲 AI 项目，面试官仍可能问工具使用与代码归属。若暂时不准备这条支线，投递版简历也应考虑弱化或删除该句。

### 2. 非目标岗位题按 JD 再恢复

- Qt 控件、信号槽、QThread、图形渲染。
- ROS、自动驾驶仿真、Binder、Android Framework。
- 摄像头、图像调优、音视频编解码。
- 驱动、中断、设备树、I2C/SPI、内核模块、eBPF。
- Go、Java、iOS、Cocos2d-x。
- Kubernetes、消息队列、微服务治理等简历未出现的深题。

这些题不是没有价值，而是对当前简历命中率低。若目标岗位 JD 明确要求，再从原始面经恢复对应专题。

## 十二、不同岗位的复习路径

| 目标岗位           | 第一优先                          | 第二优先                  | 可降低             |
| -------------- | ----------------------------- | --------------------- | --------------- |
| C++ 服务器后端      | 项目链路、C++、网络、并发、MySQL/Redis、手撕 | Linux 调试、Nginx、Docker | 嵌入式硬件题          |
| Linux/C++ 应用开发 | C++、进程线程、IPC、内存、GDB、网络        | 项目链路、数据库、Docker       | 分布式理论深题         |
| 嵌入式 Linux 应用层  | C/C++、Linux API、Socket、内存、调试  | 项目分片与故障排查             | MySQL/Redis 高级题 |
| 通用后端           | 项目一致性、网络、MySQL、Redis、算法       | C++ 对象模型、Docker       | 驱动与 Qt 题        |

## 十三、一周冲刺顺序

### 第 1 天：简历验真

- 修正或删除负载均衡、LRU-K、性能测试等无代码证据的表述。
- 写出 90 秒自我介绍和两分钟项目介绍。
- 为项目来源、个人贡献、部署规模、是否上线准备统一答案。

### 第 2 天：项目主链路

- 口述 `/api/md5`、普通上传、分片上传、分享、转存、删除五条链路。
- 每条链路都回答鉴权、数据落点、失败窗口和幂等性。

### 第 3 天：C++ 与基础构建

- 多态、智能指针、移动语义、STL、内存、编译链接。
- Makefile/CMake 以“能脱离 IDE 搭起基础多文件项目”为完成标准：会写可执行目标、静态库、头文件路径和链接关系，会切换 Debug/Release，并能定位头文件缺失与链接符号缺失。
- 面试口径如实说明平时主要使用集成 CMake 的 CLion 或 Visual Studio；高级用法可以不会，但要能解释 IDE 背后的配置、生成、编译和链接流程。
- 每个 C++ 主题至少准备一个代码例子和一个常见错误；构建部分至少亲手完成一次命令行构建和一次故障排查。

### 第 4 天：Linux 与网络

- 进程线程、IPC、锁、epoll、零拷贝、GDB。
- TCP 握手挥手、可靠性、拥塞控制、HTTPS 与故障排查。

### 第 5 天：MySQL、Redis、Nginx、Docker

- 全部结合项目真实数据结构和配置，不只背定义。
- 专门练习“数据库、缓存、文件系统三方一致性”。

### 第 6 天：手撕

- 链表 3 题、树 2 题、数组/字符串 3 题、LRU、生产者消费者。
- 每题限时并口述测试用例。

### 第 7 天：完整模拟

- 10 分钟项目深挖。
- 20 分钟 C++/Linux/网络连问。
- 20 分钟手撕。
- 10 分钟 HR 与反问。
- 复盘所有含糊数字、框架能力冒充个人实现、理想方案冒充当前实现的问题。

## 十四、30 题模拟面试顺序

1. 自我介绍。
2. 为什么选择这个项目？
3. 两分钟介绍架构和个人贡献。
4. 项目实际部署规模是什么？
5. 一次小文件上传的完整链路是什么？
6. 【源码复盘】秒传接口在当前代码中的三个分支是什么？
7. 两个用户并发上传同一文件会怎样？
8. 大文件分片怎样保存和合并？
9. 网络中断后怎样续传？当前实现有哪些边界？
10. FastDFS 上传成功、MySQL 失败怎样处理？
11. 【源码复盘】当前代码中的 MySQL 六张表如何关联？
12. Redis 保存什么？哪些数据是事实源？
13. 删除被多个用户引用的文件怎样处理？
14. 当前单点在哪里？怎样做高可用？
15. 你是否真的实现了负载均衡和 LRU-K？证据是什么？
16. C++ 动态多态怎样实现？
17. 为什么基类析构函数应设为虚函数？
18. 三种智能指针的区别是什么？
19. `shared_ptr` 是否线程安全？
20. `vector` 怎样扩容，何时迭代器失效？
21. `map` 与 `unordered_map` 怎样选择？
22. 进程和线程的区别是什么？
23. 【源码复盘】`fork/exec/pipe` 在当前项目上传链路里怎样使用？
24. 死锁的条件和排查方法是什么？
25. `select/poll/epoll` 有什么区别？
26. TCP 为什么三次握手、四次挥手？
27. TCP 粘包与业务分片乱序有什么区别？
28. MySQL 为什么使用 B+ 树？
29. Redis 为什么快，怎样持久化？
30. 手写 LRU 或反转链表，并给出完整测试用例。

完成标准不是“看过答案”，而是每题可以在 30 秒内先给结论、2 分钟内展开，并能继续承接至少两层追问。

## 十五、与现有资料的对应关系

| 资料 | 用途 |
| --- | --- |
| `真实面经.txt` | 公司与轮次的原始问题语料，保留现场问法 |
| `云存储真实面经.txt` | 云存储专项原题，包含大量重复追问 |
| `实际面经整理与原始归档.md` | 当前项目统一回答口径与代码事实边界 |
| `网盘项目架构与HTTP接口学习文档.md` | 项目实现细节、接口、数据表和风险依据 |
| `八股面试题集锦.md` | 完整问题库；本题单只负责筛选优先级 |
| `八股面试题答案整理.md` | 对应答案；项目专项为第 249～308 题 |
| `模拟面试记录.md` | 已暴露的个人答题薄弱点和复盘记录 |

本题单是“先学什么、哪些题一定会命中”的入口，不替代原始面经和详细答案文档。
