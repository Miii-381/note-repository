# 模拟面试记录 01 — FastDFS 项目 + C++ 八股摸底

> 日期:2026-09-05
> 形式:模拟面试(自我介绍 → 项目深挖 → 八股轮转)
> 简历主线:Linux C++ 后端 / FastDFS 云存储 / Nginx / MySQL / Redis

## 一、重要发现:项目真实状态与简历风险(必须处理)

面试中确认:**项目源码是读过的,但没有亲手敲过**(低于"跟着教程敲"的程度)。这是当前最大的面试风险,优先于一切八股复习。

### 关键结论

- 面试不是"把整个项目背到滴水不漏"能赢的——教程项目一追问就穿,背全套只会增加被戳穿的位置。
- 策略:**简历降险 + 选定少数纵深阵地挖到能接追问 + 其余主动降级为"了解"**。
- 面试判断标准:背下来能答一轮 ≠ 理解了能接三轮追问。简历承诺与口头能力必须一致。

### 简历高危措辞 → 建议改法

| 简历原文 | 风险 | 建议改法 |
| --- | --- | --- |
| 独立开发者 | 默认从零写的,追问必穿 | 删掉,或如实写"在参考实现基础上完成环境搭建与部署" |
| 改进了 FastDFS 的原负载均衡算法 | 承诺了没做的事 | "学习并复现论文提出的动态反馈负载均衡算法,完成逻辑验证"(复习文档口径搬回简历) |
| "项目难点"4 条 | 每条都是可被戳穿位置 | 只留真读懂、能接追问的 1~2 条,其余降级为功能描述 |

### 三轮模拟暴露的答题弱点

- **能背流程,但失败窗口 / 一致性 / 事务边界答不深**(分享链路问"中间一步失败留下什么"时露怯)。
- **反复踩同一个坑**:一遇到"Redis 数据丢了"就条件反射答"回 MySQL 确认",不先判断这份数据有没有 MySQL 权威版本。

## 二、核心框架收获:Redis 数据的 A / B 权威源判断

这是本轮最有价值的方法论,必须内化。

### 判断标准(唯一一条)

> **Redis 里这份数据,MySQL 此刻查得到权威版本吗?**
> 查得到 = A(缓存副本);查不到 = B(Redis 自己就是权威)。

### 两种角色

| | A — 缓存副本 | B — 中间状态 / 独立权威 |
| --- | --- | --- |
| 例子 | 共享文件排行(ZSet),是 share_file_list 的投影 | 登录 Token(MySQL 无权威投影);分块上传的预期 MD5 暂存 |
| 丢了会怎样 | 回源 MySQL 重建即可 | 真丢了,只能靠重试 / 超时 / 清理任务兜底,**没有回源重建这条路** |
| "回查 MySQL 确认"有效吗 | 有效 | 无效——MySQL 无米下锅,权威版本尚未产生或根本不存 |

### 正确理解"Redis 是缓存"

- MySQL 是权威,Redis 是副本,可随时清空重建。
- "缓存未命中 ≠ 不存在",未命中必须回源,绝不把"未命中"当结论。
- 判断"是否已分享"唯一正确答案在 MySQL;查 Redis 只是加速。

### 本轮三个实例(含我的错误示范,均已掰正)

1. **分享(蒙对)**:MySQL 提交成功、写 Redis 前崩溃 → 不危险,因 ZSet 未命中会回查 MySQL 命中并回填。Redis 只存"已分享文件",没有"未分享"状态位。
2. **分块上传预期 MD5(错)**:Redis 暂存的预期 MD5 在 MySQL 无权威版本,正文上传成功但暂存丢失 → 校验失败拒绝落库 → 正文成**孤儿文件**,占存储无记录,正常删除机制(引用计数归零)清不掉。是真实泄漏,不是"回源能救"。
3. **登录 Token(错)**:MySQL 里无 Token 的权威投影 → 不是 A。Token 是 Redis 作为权威的状态存储,Redis 崩溃 → 所有在线用户掉线,只能重新登录。

> 注:SETEX 在项目中的真实用途(秒传 vs 分块)存在文档冲突,待取得源码核对后再回来校正。欠账:分块/秒传链路逐段过 A/B 框架 + B 场景各自兜底方式。

## 三、负载均衡诚信口径(追问要点,背熟)

- 开场主动澄清"不是原创、论文复现"——这步做对了,但简历措辞与口头口径不一致会翻车。
- **禁止**:把论文实验结论说成个人实测。个人无原始压测记录,不能说提升 20~30%。
- 准确描述边界:"本地以独立模块复现并验证算法流程,未改动 FastDFS 官方 Tracker 源码,未在生产运行。"
- 候选集选出后是**按处理能力做带权随机选择**(概率高 = 更可能被选中),不是"取概率最高的节点"。
- 选"最低负载进候选集"保证候选集非空,避免全负载高时无节点可用。

## 四、项目加分表达(分享机制一句话口径)

> 判断文件是否已分享,权威在 MySQL 的 share_file_list,Redis ZSet 只是加速读的正缓存。就算分享写进 MySQL 成功、写 Redis 前进程崩了,也不会出错——新请求查 ZSet 未命中会回查 MySQL,命中后回填缓存。唯一要保证的是:缓存未命中永远回源,绝不把"未命中"当结论。

## 五、八股摸底:四题表现记录

> 水位判断:C++ 主战场扎实,操作系统有一处硬伤,网络是强项。

### 1. C++ 移动语义(std::move)

- 答对:move 只做无条件类型转换 `static_cast<T&&>`(贴"可移动"标签),移动动作由移动构造/赋值执行;move 后对象"有效但未指定",可析构可重新赋值,不能假设内部值。
- 需补:
  - 标签的准确类型叫**右值引用 `T&&`**。
  - "未指定"≠乱,保证可析构、可重新赋值;但标准不保证移动后还能做有意义的"比较"。
  - 价值 = 避免深拷贝:move 是移交内部指针,拷贝是复制整段数据,大对象省一次 O(n) 拷贝。
  - `std::move` 本身零运行时开销,是编译期类型转换。

### 2. vector 扩容 + 迭代器失效

- 答对:cap 满时申请更大内存(1.5~2 倍几何增长),旧元素移/拷到新址,原地构造新元素,释放旧内存;整块新地址 → 迭代器/指针/引用全部失效。string:任何触发重分配的操作使 c_str() 指针失效。
- 需补:
  - 扩容时"移动还是拷贝"取决于元素移动构造是否 **noexcept**:有则移动,可能抛异常则退化为拷贝(强异常安全保证)。
  - 几何增长(而非线性)目的是让 push_back **摊还 O(1)**。
  - C++17 保证 c_str() 与 data() 同地址,不触发重分配则一直有效。

### 3. 进程 / 线程(含一处硬伤,必纠)

- 答对:进程=资源分配单位,线程=CPU 调度单位;进程切换开销大线程小;共享代码段/堆/文件描述符,各自拥有栈/寄存器/局部数据。
- **硬伤 1(重点纠错)**:线程切换"全程用户态"是**错的**。标准库线程是内核线程,调度由内核负责,**同样陷入内核做上下文切换**。线程轻 ≠ 不进入内核,而是**切换时地址空间不变、不用换页表,省掉换页表 + TLB 冲刷**。说"全程用户态"的是**协程/用户态线程**的特征,别和普通线程搞混。
- **硬伤 2**:通信简单是共享地址空间的便利,不是线程"轻"的原因;"轻"指创建和切换开销低,两个维度别搅一起。
- 补精确:两线程不共享栈,共享堆、代码段、全局/静态数据、fd 表、信号处理等;各自独占栈、寄存器上下文、TLS。
- 备注:教材"状态转换=变态"是正规术语,此词无误(指状态变换)。

### 4. TCP 三次握手(四题中答得最深入)

- 答对核心:**历史连接**问题——旧 SYN 迟到,两次握手会让服务端建立无效连接;三次握手让客户端能回 RST 拒绝。
- 需补,三点合一才是满配:
  1. 历史连接:服务端资源被一个永远等不到确认的无效半开连接占住(不是"白白发数据",是资源占用)。
  2. 双向通信确认:三次让两端都确认收发通;两次只确认了客户端→服务端方向。
  3. 资源浪费早回收:第三次握手让服务端有机会及早回收无效连接资源。

## 六、当前缺口清单(下次复习目标)

- [ ] 简历措辞整改(独立开发者 / 改进算法 / 难点列表)
- [ ] SETEX 真实用途核源码后,回补分块/秒传链路的 A/B 分析
- [ ] 操作系统:线程切换真正开销来源(地址空间不变省换页表/TLB),进程/线程/协程切换开销对比
- [ ] 分享/取消分享/转存/删除各自的失败窗口与数据生命周期
- [ ] 八股未测区:TCP 四次挥手、流量/拥塞控制、MySQL(索引/事务/MVCC/锁)、Redis(数据结构/持久化)

## 七、下次模拟面试开场建议

1. 自我介绍务必讲:教育背景 + **"独立开发者"如实化处理后的项目身份** + 挑 1~2 个最有难度的功能**主动展开**(不要报菜名),并收在"为什么选这个方案"。
2. 项目只讲能接追问的阵地,其余 30 秒讲完主动刹车、明确说"这块实现浅,可以考我链路/秒传那块"。
3. 主动圈地 vs 被戳穿——前者叫诚实有自知,后者叫造假被抓。
